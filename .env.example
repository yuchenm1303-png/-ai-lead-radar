# Web / allowed origins
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
CORS_ORIGINS=https://smirel.com,https://www.smirel.com,http://localhost:3000,http://127.0.0.1:3000

# Storage: local/test uses SQLite. Production may set DATABASE_URL to a managed Postgres URL.
DATABASE_PATH=./lead_radar.db
DATABASE_URL=

# Automatic source connectors. Keep mock for local verification; production can leave empty until an approved source is configured.
SCAN_CONNECTORS=mock

# Optional protection for write endpoints. Never commit the real token.
RADAR_WRITE_TOKEN=

# AI provider. Keep rules for zero-cost/local mode; set openai only when a key + model are configured.
AI_PROVIDER=rules
OPENAI_API_KEY=
OPENAI_MODEL=

# Notifications
FEISHU_WEBHOOK_URL=
NOTIFY_MIN_SCORE=85
