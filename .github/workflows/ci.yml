name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  web:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install
      - run: npm run build

  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements.txt
      - run: python -m pip install --upgrade pip
      - run: pip install -r requirements.txt
      - run: python -m compileall app benchmarks collectors
      - run: python -m unittest discover -s tests -v

  edge-functions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
      - run: deno check supabase/functions/_shared/lead_policy.ts
      - run: deno check supabase/functions/lead-radar-ingest/index.ts
      - run: deno check supabase/functions/lead-radar-collector/index.ts
      - run: deno check supabase/functions/lead-radar-scan/index.ts
      - run: deno test supabase/functions/_shared/lead_policy_test.ts
      - run: deno test supabase/functions/lead-radar-scan/preview_contract_test.ts

  browser-helper:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node --check browser-helper/popup.js
      - run: node -e "const fs=require('fs'); const m=JSON.parse(fs.readFileSync('browser-helper/manifest.json','utf8')); if(m.manifest_version!==3) process.exit(1);"
